<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>netcoreapp3.1</TargetFramework>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <DefineConstants></DefineConstants>
  </PropertyGroup>

  <Target Name="PreBuild" BeforeTargets="PreBuildEvent" Condition="'$(Configuration)'=='Debug'">
    <ItemGroup>
      <appsettings Include="E:\Projetos\web\netcore-configmap\des\appsettings.json"/>
      <configmap Include="$(ProjectDir)/configmap" />
    </ItemGroup>
    <RemoveDir Directories="@(configmap)" />
    <Copy SourceFiles="@(appsettings)" DestinationFolder="@(configmap)" />
    <Message Text="Arquivo de configuração copiado"/>
  </Target>

</Project>
