<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>netcoreapp3.1</TargetFramework>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <DefineConstants></DefineConstants>
  </PropertyGroup>

  <Target Name="PreBuild" BeforeTargets="PreBuildEvent" Condition="'$(Configuration)'=='Debug'">
    <Copy SourceFiles="E:\Projetos\web\netcore-configmap\des\appsettings.json" DestinationFolder="$(ProjectDir)/configmap" />
    <Message Text="Arquivo de configuração copiado"/>
  </Target>

  <!--<Target Name="PreBuild" AfterTargets="Clean" Condition="'$(Configuration)'=='Debug'">
    <RemoveDir Directories="$(ProjectDir)/configmap" />
    <Message Text="Arquivo de configuração deletado"/>
  </Target>-->

</Project>
